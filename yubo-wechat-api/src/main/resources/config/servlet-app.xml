<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

	<!-- 配置文件 -->
	<context:property-placeholder location="classpath:app.properties"
		ignore-resource-not-found="true" ignore-unresolvable="true"
		file-encoding="UTF-8" />

	<!-- 扫描 -->
	<context:component-scan base-package="com.yunhou.search.page.**">
		<context:exclude-filter type="regex"
			expression=".*TestCase" />
	</context:component-scan>

	<!-- BMMS资源加载 -->
	<bean id="phpTemplate" class="com.yunhou.search.page.freemarker.PHPTemplateMethod"></bean>
	<bean id="resourceClient"
		class="com.bubugao.resource.client.freemarker.ResourceClientFunction" />


	<bean id="freemarkerConfig"
		class="org.springframework.web.servlet.view.freemarker.FreeMarkerConfigurer">
		<property name="templateLoaderPath" value="/WEB-INF/view/" />
		<property name="freemarkerVariables">
			<map>
				<entry key="statics"
					value="#{T(freemarker.ext.beans.BeansWrapper).getDefaultInstance().getStaticModels()}"></entry>
				<entry key="getResourceUrl" value-ref="resourceClient" />
				<entry key="php" value-ref="phpTemplate" />
			</map>
		</property>
		<property name="freemarkerSettings">
			<props>
				<prop key="template_update_delay">3600000</prop>
				<prop key="default_encoding">UTF-8</prop>
				<prop key="number_format">0.##########</prop>
				<prop key="datetime_format">yyyy-MM-dd HH:mm:ss</prop>
				<prop key="classic_compatible">true</prop>
				<prop key="template_exception_handler">ignore</prop>
			</props>
		</property>
	</bean>
	<bean id="viewResolver"
		class="org.springframework.web.servlet.view.freemarker.FreeMarkerViewResolver">
		<property name="cache" value="false" />
		<property name="contentType" value="text/html;charset=UTF-8" />
		<property name="requestContextAttribute" value="request" />
	</bean>
	<bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver" />

	<!-- <bean id="appSecurityInterceptor" class="com.bubugao.framework.mvc.interceptor.AppSecurityInterceptor"/> -->
	<!--<bean id="sellerAuthInterceptor" class="com.bubugao.shop.web.mvc.interceptor.SellerAuthInterceptor"/> -->

	<!-- 异步接口JSON格式化输出 -->
	<mvc:annotation-driven>
		<mvc:message-converters>
			<bean
				class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
				<property name="objectMapper"
					value="#{T(com.bubugao.framework.util.UtilJson).JSON_MAPPER}" />
			</bean>
		</mvc:message-converters>
	</mvc:annotation-driven>

	<!-- 拦截器 -->
	<mvc:interceptors>
		<ref bean="bubugaoSessionService" />
		<mvc:interceptor>
			<mvc:mapping path="/**" />
			<ref bean="paramHandler" />
		</mvc:interceptor>
	</mvc:interceptors>
	<bean id="paramHandler" class="com.yunhou.search.page.interceptor.ParamHandler">
	</bean>

	<!-- 资源过滤 -->
	<mvc:resources mapping="/res/**" location="/res/" />
	<mvc:resources location="/favicon.ico" mapping="/favicon.ico" />
	<mvc:resources location="/animated_favicon.gif" mapping="/animated_favicon.gif" />
	<mvc:resources location="/js/" mapping="/js/**"></mvc:resources>
	<mvc:resources location="/swf/" mapping="/swf/**"></mvc:resources>
	<mvc:resources location="/index.html" mapping="/index.html"></mvc:resources>
</beans>
